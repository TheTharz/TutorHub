class Post {
  int id;
  String title;
  String description;
  String subject;
  String grade;
  String image;

  Post(this.id, this.title, this.description, this.subject, this.grade,
      this.image);
}
